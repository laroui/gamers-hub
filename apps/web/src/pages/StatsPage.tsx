export function StatsPage() {
  return (
    <div style={{ color: "var(--gh-text2)", fontFamily: "var(--font-display)", letterSpacing: "1px" }}>
      Page stub — implemented in later batch
    </div>
  );
}
